import { styles } from '@/constants/theme';
import { Clube } from '@/types';
import { View } from 'react-native';

interface OverlayNovoEventoProps {
    clube: Clube;
}

export default function OverlayNovoEvento({ clube }: OverlayNovoEventoProps) {
    <View style={styles.containerTop}>
        
    </View>
}
